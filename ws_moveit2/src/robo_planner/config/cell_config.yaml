robo_planner:
  ros__parameters:
    # Robot position parameters
    positions:
      # BEMI 1 position
      bemi1:
        x: 0.3634  # X-coordinate of BEMI 1
        y: 0.6815  # Y-coordinate of BEMI 1
        z: 0.320   # Z-coordinate of BEMI 1
        RZ_offset: 0.0475 # Z-rotation offset of BEMI 1 
      # BEMI 2 position
      bemi2:
        x: 0.0  # X-coordinate of BEMI 2
        y: 0.0   # Y-coordinate of BEMI 2
        z: 0.0  # Z-coordinate of BEMI 2
        RZ_offset: 0.0 # Z-rotation offset of BEMI 2 

      # BEMI 3 position
      bemi3:
        x: 0.0
        y: 0.0
        z: 0.0
        RZ_offset: 0.0 # Z-rotation offset of BEMI 3 
      
      # Part Carrier 1, x offset=10.5cm
      PC_1:
        x: [-0.351, -0.456, -0.561, -0.666, -0.771]
        y: [0.341, 0.341, 0.341, 0.341, 0.341] 
        z: 0.26      

      # Station positions
      station:
        y_approach: -0.40759  # Y-coordinate for approach position
        y_execute: -0.55759   # Y-coordinate for execution position
        x_positions: [-0.189, -0.0167, 0.1375, 0.29433]  # X-coordinates for different stations
    
    # Robot speed parameters 
    # TODO not implemented in code yet
    speeds:
      ptp: 0.3               # Point-to-Point movement speed
      cartesian_fast: 0.1    # Fast Cartesian movement speed
      cartesian_slow: 0.03   # Slow Cartesian movement speed
    
    # Timing parameters (in milliseconds)
    timings:
      default_wait: 1000     # Default wait time
      gripper_close: 1000    # Time to wait for gripper to close
    
    # Deburring process parameters
    deburring:
      circle_radius: 0.023   # Radius of deburring circle
      z_offset: 0.03         # Z-axis offset for deburring
    
    # Safety parameters
    safety:
      placement_offset_z: 0.2  # Z-axis offset for safe placement
    
    # Trajectory parameters
    trajectory:
      circle:
        radius: 0.1          # Radius of circular trajectory
        center: [0.3, 0.3, 0.4]  # Center point of circular trajectory [x, y, z]